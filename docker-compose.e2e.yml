services:
  consumer:
    environment:
      PAYMENT_PROVIDER_REQUEST_TIMEOUT_SECONDS: "1"
      PAYMENT_RECONCILIATION_STALE_AFTER_SECONDS: "2"
      PAYMENT_RECONCILIATION_POLL_INTERVAL_SECONDS: "0.5"
      OUTBOX_RELAY_POLL_INTERVAL_SECONDS: "0.2"
